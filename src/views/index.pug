extends layout

block content
  h1= title
  p Welcome to #{title}

  p
    a(href='/book') Books
  button(id='save-books-button') Save books

block scripts
  script(src='/scripts/index.js' type='module')